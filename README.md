# vishwjeettomar.github.io

This repository contains the static website for *Vishwjeet Singh Tomar* — Independent Consultant.

## To publish with GitHub Pages
1. Create a repository named `vishwjeettomar.github.io` under your GitHub account.
2. Clone the repo and copy the site files into it.
3. Commit and push to `main` branch:
```bash
git add .
git commit -m "Initial site upload"
git push origin main
```
4. Site will be available at: `https://vishwjeettomar.github.io` shortly.

## Analytics
Replace `G-XXXXXXXXXX` in the HTML head with your Google Analytics 4 Measurement ID.

## Notes
- The contact form uses `mailto:` to open the user's email client. If you want a server-side handler, I can add a simple Node/PHP script.